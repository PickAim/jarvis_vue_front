import { ResultCode } from "@/packages/ResultCode";
export default class StorageHandlerClass {
    // TODO: make storage
    getValue(key) {
        return "";
    }
    setValue(key, value) {
        return ResultCode.OK;
    }
}
